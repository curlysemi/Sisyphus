using ByteDev.DotNet.Project;
using System;
using System.Collections.Generic;
using System.Text;

namespace Sisyphus.Helpers
{
    public static class ProjectFileHelper
    {
        public static KeyValuePair<string, string> GetNamePathMappingFromProjectFile(string projectFilePath, string relativeToPath)
        {

            //var project = DotNetProject.Load(projectFilePath);
            //if (project != null)
            //{

            //}

            throw new NotImplementedException();
        }
    }
}
